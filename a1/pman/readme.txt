readme.txt

To compile PMan, type 'make PMan' in the command prompt.
Usage of PMan is according to specifications.
To exit PMan, enter 'exit'. If there are processes active,
PMan will ask if the user wishes to leave those processes running
after exiting. If yes, PMan will kill all remaining processes. If no,
these processes will be left active.
